---
# defaults file for sorensj.nfs

# List of NFS shares
nfs_share_mounts: []

# Default NFS4 mount options
nfs_mount_opts: defaults

# Default NFS version (nfs for v3)
nfs_version: nfs

nfs_rpcbind_state: started
nfs_rpcbind_enabled: true
